<<<<<<< HEAD
gui_window:
  title: MIISLAB
  min_width: 900
  min_height: 480
  default_open_dir: ./

model:
  classifier_name: livecc_7b

openai_tts:
  model_url: "wss://api.openai.com/v1/realtime?model=gpt-4o-realtime-preview"
  default_voice: coral
  default_speed: 1.5
  temperature: 0.7

chatterbox_tts:
  audio_prompt_path: /home/miislab/livecc_gui/ref_voice/announcer_ref.wav
  temperature: 0.7
  cfg_weight: 1.0
  chunk_size: 25

gemini_tts:
  model_name: "models/gemini-3.1-flash-tts-preview"
  voice: Kore

gemini:
  model_name: gemini-3.1-flash-lite-preview
  rag_threshold: 600
  temperature: 0.4
  max_output_tokens: 50

inference:
  dedup_window_s: 3.0
  dedup_threshold: 0.75
  max_pending: 400
=======
gui_window:
  title: MISLAB
  min_width: 1200
  min_height: 500
  default_open_dir: ./

model:
  classifier_name: livecc_7b

# Remote inference (this GUI is a thin client — no local LiveCC / VLM on this machine).
# Set client_only: false to restore the legacy path that loads LiveCC and runs inference locally
# (requires a powerful GPU and full requirements).
remote:
  enabled: true
  client_only: true
  host: 127.0.0.1
  port: 9000

# Audience second-screen (HTTP page starts with GUI; LiveKit publisher needs Free Switch)
# Start LiveKit:  docker compose up -d
# Audience page:  http://localhost:8080/audience  (same LAN: use Wi-Fi IPv4 from ipconfig, e.g. http://10.x.x.x:8080/audience)
# livekit_url must be reachable from the *viewer* device — not WSL/Hyper-V only (172.x.x.x).
audience:
  enabled: true
  livekit_url: "ws://10.207.3.31:7880"
  api_key: "devkey"
  api_secret: "miis_broadcast_local_dev_secret_key_01"
  room: "broadcast-room"
  port: 8080       # audience token server / viewer page port
>>>>>>> Multi-API
